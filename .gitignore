node_modules/
dist/
.vite/
.env
.env.*
!.env.example
.DS_Store

# High-resolution/raw generation inputs stay local unless deliberately archived elsewhere.
assets-src/raw/

# Optional atlas inspection output is not shipped by default.
.asset-build/
