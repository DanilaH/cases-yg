name: Apply pre-DRAFT UI/debug correction

on:
  push:
    branches:
      - fix/pre-draft-ui-debug

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 22.12.0
          package-manager-cache: false
      - run: npm ci
      - name: Apply correction
        run: node .github/scripts/apply-pre-draft-ui-debug.mjs
      - name: Verify product change
        run: |
          npm run typecheck
          npm test
          npm run assets:selftest
          npm run assets:validate
          npm run build
          git diff --check
      - name: Commit product changes
        run: |
          if git diff --quiet -- src tests; then
            echo 'Product patch already applied.'
            exit 0
          fi
          git config user.name 'DanilaH'
          git config user.email '41333602+DanilaH@users.noreply.github.com'
          git add src tests
          git commit -m 'Fix pre-DRAFT reward layout and debug seeds'
          git push origin HEAD:fix/pre-draft-ui-debug
