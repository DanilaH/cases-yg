name: Apply mobile acceptance hardening

on:
  push:
    branches:
      - mobile-acceptance-hardening

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: mobile-acceptance-hardening
      - uses: actions/setup-node@v6
        with:
          node-version: 22.12.0
          package-manager-cache: false
      - name: Apply source changes
        id: apply
        continue-on-error: true
        run: node tools/apply-mobile-acceptance-hardening.mjs > mobile-patch-output.txt 2>&1
      - name: Persist patch diagnostic on failure
        if: steps.apply.outcome == 'failure'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mobile-patch-output.txt
          git commit -m "chore: capture mobile patch failure"
          git push origin HEAD:mobile-acceptance-hardening
      - name: Stop after patch failure
        if: steps.apply.outcome == 'failure'
        run: |
          cat mobile-patch-output.txt
          exit 1
      - name: Remove one-shot patch machinery
        if: steps.apply.outcome == 'success'
        run: rm -f mobile-patch-output.txt tools/apply-mobile-acceptance-hardening.mjs .github/workflows/mobile-acceptance-apply.yml
      - name: Install dependencies
        if: steps.apply.outcome == 'success'
        run: npm ci
      - name: Typecheck
        if: steps.apply.outcome == 'success'
        run: npm run typecheck
      - name: Unit tests
        if: steps.apply.outcome == 'success'
        run: npm test
      - name: Asset self-test
        if: steps.apply.outcome == 'success'
        run: npm run assets:selftest
      - name: Asset validation
        if: steps.apply.outcome == 'success'
        run: npm run assets:validate
      - name: Production build
        if: steps.apply.outcome == 'success'
        run: npm run build
      - name: Commit validated implementation
        if: steps.apply.outcome == 'success'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Harden mobile layout and reveal presentation"
          git push origin HEAD:mobile-acceptance-hardening
