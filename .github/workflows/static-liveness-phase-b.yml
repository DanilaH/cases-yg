name: Static liveness phase B

on:
  push:
    branches:
      - feature/static-scene-liveness

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/static-scene-liveness
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - name: Apply phase B
        run: node scripts/apply-static-liveness-phase-b.mjs
      - name: Verify
        run: |
          npm ci
          npm run typecheck
          npm test
          npm run assets:selftest
          npm run assets:validate
          npm run build
      - name: Commit source changes and clean one-shot files
        run: |
          rm -f scripts/apply-static-liveness-phase-b.mjs .github/workflows/static-liveness-phase-b.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add pointer-reactive material response"
          git push origin HEAD:feature/static-scene-liveness
