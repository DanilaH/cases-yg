name: Analytics final pass

on:
  push:
    branches:
      - analytics/final-event-pass

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: analytics/final-event-pass
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Apply analytics event pass
        run: node tools/analytics-final-pass-temp.mjs
      - name: Targeted analytics validation
        run: |
          npm run typecheck
          npx vitest run tests/analytics.test.ts
      - name: Remove temporary finalizer and commit production diff
        shell: bash
        run: |
          rm -f tools/analytics-final-pass-temp.mjs .github/workflows/analytics-final-pass-temp.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Add final product analytics events"
          git push origin HEAD:analytics/final-event-pass
